

const defaultState = {
    currentUser: {},
    isAuthorised: false
}
export default function userReducer(state = defaultState,  action){
    switch (action.type){

        default:
            return state;
    }
}